# 问题

## 1.logger.py中日志的创建

查看``app/core/logger.py``的注释

## logger的分层使用是怎么做的，yield不是流式的吗，为什么最外层能try，使用logger

```python
    try:
        systemp_prompt = prompt_manager.load_prompt(request.prompt_type)
        memory = JsonMemory(session_id=request.session_id)
        history = memory.load_history()

        messages = build_message(
            systemp_prompt=systemp_prompt, history=history, user_input=request.message
        )

        def generate():
            full_reply = ""

            try:
                # 那边每yield一次，这边就循环一次
                for chunk in llm.stream_chat(messages):
                    full_reply += chunk
                    yield chunk

                memory.add_message("user", request.message)
                memory.add_message("assistant", full_reply)

                logger.info("流式聊天完成:session_id=%s", request.session_id)

            except Exception as e:
                logger.error("流式输出异常:%s", e)
                yield f"\n[ERROR] {str(e)}"

        # 流式类型，让前端显示出流式
        return StreamingResponse(generate(), media_type="text/plain; charset=utf-8")
    except Exception as e:
        logger.error("流式聊天接口异常:%s", e)
        raise HTTPException(status_code=500, detail=str(e))
```

因为最外层只是先定义了generate函数，并没有调用它，在return时才会调用。

* 还没return就异常，此时存入日志（接口异常）

* return后有问题，和接口无关，此时判断异常由内部的try进行

# 总结

工程化项目中，很多东西为了方便/易记，会进行统一，不只追求当下方便，也要考虑后续维护的简易程度，照顾全局  


